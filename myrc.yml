apiVersion: v1
kind: ReplicationController
metadata: 
 name: my-first-rc
spec:
 replicas: 3
 selector: 
  app: mynginx
 template:
   metadata:
    labels:
     app: mynginx
   spec:
    containers:
    - name: my-first-container
      image: nginx
      ports:
        - containerPort: 80  