apiVersion: v1
kind: ReplicationController
metadata: 
 name: my-first-rc
spec:
 replicas: 3
 selector: 
  app: mynginx
 template:
  metadata:
   name: my-second-pod
  spec:
    containers:
    - name: my-first-container
      image: nginx
      ports:
        - ContainerPort: 80  